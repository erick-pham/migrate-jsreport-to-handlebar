<html>

<head>
  <style>
    .main {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      width: 100%;
      height: 100%;
      /* position: relative;
    top: 0;
    left: 0; */
    }

    .border-header {
      border-top: 10px solid #f07662;
    }

    .footer {
      width: 95%;
      margin-left: 25px;
      margin-bottom: 15px;
    }

    .footer-main {
      display: flex;
      flex-direction: row;
      justify-content: flex-start;
      width: 100%;
      margin-right: 25px;
      margin-bottom: 15px;
    }

    .footer-text-secondary {
      color: #00004d;
      font-size: 10px;
      width: 15%;
      margin: 5px;
      text-align: right;
    }
  </style>
</head>

<body>
  {{#each $pdf.pages}}
  {{#if @index}}
  <div style="page-break-before: always;"></div>
  {{/if}}
  <main class="main">
    <header class="header border-header">
      {{!-- <div class="footer-main">
        <img src="{#asset images/logo.png @encoding=dataURI}"
          style="width:100%; max-width:100px; align-self:right; margin-top: 15px; margin-left: 35px" />
      </div> --}}
    </header>
    <footer class="footer-main" style="justify-content: flex-end;">
      <div class="footer-text-secondary">
        <span>{{getPageNumber @index}} of {{getTotalPages ../$pdf.pages}}</span>
      </div>
    </footer>
  </main>
  {{/each}}
</body>

</html>